# SEO 关键词挖掘与实战准则

当需要寻找“高搜索、低竞争”的关键词，且无法直接访问官方付费工具（如 Google Keyword Planner）时，必须执行以下数据驱动的发现流程。

## 1. 核心挖掘策略：Google Autocomplete 意图探测
- **原理**：Google 的搜索建议词（Autocomplete）是基于真实海量搜索数据的实时反馈。
- **执行方法**：编写并运行脚本（Node.js 或 Python）调用 `https://suggestqueries.google.com/complete/search?client=firefox&q={keyword}` 接口。
- **分析重点**：
  - **后缀分析**：观察核心词后的联想词。例如搜索 `Maextro car` 出现 `s800 price`, `interior`，说明全球用户对价格和细节极度渴求。
  - **地缘分析**：观察是否有特定国家后缀（如 `in canada`, `in australia`），这决定了文章的市场定位。

## 2. 关键词筛选逻辑：三维过滤法
- **维度一：搜索量 (Demand)** 
  - 现象：建议词列表中排名靠前的词。
  - 判定：如果一个新品牌（如 Maextro）已经出现了具体型号的建议词，说明该型号已有爆发性流量。
- **维度二：竞争度 (Competition)**
  - 判定：在 Google 搜索该关键词，如果排名前五的结果中没有大型主流媒体（如 CNN, BBC, Reuters）的专项深度报道，则判定为“低竞争”。
- **维度三：关联度 (Relevance)**
  - 判定：关键词必须能承载博主的核心观点（如“法律风险” vs “Maextro 价格”）。

## 3. 落地实施：文章结构优化
- **标题组合 (Slug & Title)**：标题必须包含“高热度搜索词”+“高争议性话题”。例如：`Maextro S800 Price and the Legal Risk...`
- **LSI 关键词布局**：在正文中自然分布抓取到的联想词。不要堆砌，要在子标题和首尾段落中体现。
- **内链权重**：通过旧有相关文章的内链，提升新页面的权重。

## 4. 自动化脚本模板 (Node.js)
```javascript
const https = require('https');
const keywords = ['核心关键词1', '核心关键词2'];
keywords.forEach(kw => {
  https.get(`https://suggestqueries.google.com/complete/search?client=firefox&q=${encodeURIComponent(kw)}`, (res) => {
    let data = '';
    res.on('data', chunk => data += chunk);
    res.on('end', () => console.log(`${kw}: ${data}`));
  });
});
```

## 5. 周期性自检
- **严禁靠想象**：所有关键词必须有脚本抓取结果作为支撑。
- **拒绝 AI 常用词**：避免使用 `Transformative`, `Crucial` 等 AI 喜欢的抽象词作为关键词。
- **关注动态**：由于汽车行业变化极快，每次开启 New Task 时应重新运行脚本获取最新建议词。
